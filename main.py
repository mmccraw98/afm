# This is a sample Python script.
import numpy as np
import matplotlib.pyplot as plt
# Press Ctrl+Enter to execute it or replace it with your code.
# Press Double Shift to search everywhere for classes, files, tool windows, actions, and settings.

from afm.data_io import ForceMap
from afm.misc import get_line_point_coords

# Press the green button in the gutter to run the script.
if __name__ == '__main__':
    # a = ForceMap('/home/mmccraw/Desktop/gwu/dish2t/ForceMap07')
    # a.flatten_and_shift(left_right_mask=[0.1, 0.9], up_down_mask=[0.01, 0.4], order=1)
    # a.plot_map()
    pass